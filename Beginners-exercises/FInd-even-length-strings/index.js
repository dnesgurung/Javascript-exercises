function findEvenLengthStrings(items){

const result = [];
  for (let i = 0; i < iems.length; i++ ){
    if(typeof items[i] === 'string' && items[i].length % 2 === 0){
      result.push(items[i];
    }
  }
  return result;
}
